# Copy to .env (never commit it) — or export these in your shell.
RAFID_BASE_URL=https://api.rafidsystem.com
RAFID_API_KEY=raf_live_replace_me
# Optional: auto (default) | credits | subscription
RAFID_PAYMENT_METHOD=auto
