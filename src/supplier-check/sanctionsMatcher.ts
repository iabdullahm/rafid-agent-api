/** Moved to src/shared/sanctions/nameMatching.ts (shared with company_reputation_check).
 *  Re-exported unchanged so existing imports keep working. */
export * from "../shared/sanctions/nameMatching.js";
